package components

import (
	"main/web/app/types"
)

templ Navbar(webconfig types.WebConfig) {
    <nav class="bg-white shadow-md">
        <div class="container mx-auto px-4 py-4">
            <h1 class="text-2xl font-bold">{ webconfig.Translations["nav.home"] }</h1>
        </div>
    </nav>
}
